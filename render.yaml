services:
  - type: web
    name: xamton-relay
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: XAMTON_DB_PATH
        value: /opt/render/project/src/xamton.db
    rootDir: backend
